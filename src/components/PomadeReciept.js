import { Card } from 'react-bootstrap';
import { useContext } from 'react';
import ProductContext from '../ProductContext';

const PomadeReceipt = () => {
    return (
        <Card>
            
        </Card>
    )
}

export default PomadeReceipt.apply;